[package]
name = "template-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
tera = "1"
rand = { version = "0.8", features = ["getrandom"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
flate2 = "1"
